name: pmsec release npm

on:
  push:
    tags:
      - 'pmsec-node-v*'

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: node
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org/
      - name: Upgrade npm to a release that supports OIDC trusted publishing
        run: npm install -g npm@latest
      - name: Verify tag matches package.json version
        run: |
          tag_version="${GITHUB_REF_NAME#pmsec-node-v}"
          pkg_version=$(node -p "require('./package.json').version")
          [ "$tag_version" = "$pkg_version" ] || { echo "tag $tag_version != package.json $pkg_version" >&2; exit 1; }
      - run: node --test
      - run: npm publish --provenance --access public
